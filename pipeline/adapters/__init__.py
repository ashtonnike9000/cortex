"""
Source adapters for the Cortex understanding system.

Each adapter reads a specific device/format and emits canonical signal records.
The pipeline doesn't care where the data came from — only that it conforms
to the shared schema after adaptation.
"""
